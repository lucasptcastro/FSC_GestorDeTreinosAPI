// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

enum WeekDay {
  SUNDAY
  MONDAY
  TUESDAY
  WEDNESDAY
  THURSDAY
  FRIDAY
  SATURDAY
}

model User {
  id           String        @id @default(uuid())
  workoutPlans WorkoutPlan[]
}

model WorkoutPlan {
  id          String       @id @default(uuid())
  name        String
  userId      String
  user        User         @relation(fields: [userId], references: [id]) // relação entre WorkoutPlan e User, onde userId é a chave estrangeira que referencia o id do User
  isActive    Boolean      @default(true)
  createdAt   DateTime     @default(now()) // timestamp com dia/mês/ano e hora da criação
  updatedAt   DateTime     @updatedAt
  workoutDays WorkoutDay[]
}

model WorkoutDay {
  id            String            @id @default(uuid())
  name          String
  workoutPlanId String
  workoutPlan   WorkoutPlan       @relation(fields: [workoutPlanId], references: [id]) // relação entre WorkoutDay e WorkoutPlan, onde workoutPlanId é a chave estrangeira
  isRest        Boolean           @default(false) // campo para indicar se o dia é de descanso
  weekDay       WeekDay
  exercises     WorkoutExercise[]
  createdAt     DateTime          @default(now())
  updatedAt     DateTime          @updatedAt
}

model WorkoutExercise {
  id                String     @id @default(uuid())
  name              String
  order             Int
  workoutDayId      String
  workoutDay        WorkoutDay @relation(fields: [workoutDayId], references: [id]) // relação entre WorkoutExercise e WorkoutDay, onde workoutDayId é a chave estrangeira
  sets              Int
  reps              Int
  restTimeInSeconds Int
  createdAt         DateTime   @default(now())
  updatedAt         DateTime   @updatedAt
}
